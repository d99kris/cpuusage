//unsupported (internally used by cpuusage): void va_start(va_list ap, argN);
//unsupported (internally used by cpuusage): void va_copy(va_list dest, va_list src);
//unsupported (internally used by cpuusage): type va_arg(va_list ap, type a);
//unsupported (internally used by cpuusage): void va_end(va_list ap);
